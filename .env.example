# Copy this file to .env.local and fill in the values.

# The public-facing URL of this application (used for generating links, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database connection string (required for real token verification)
# DATABASE_URL=
